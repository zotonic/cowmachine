{erl_opts, [debug_info]}.

{plugins, [
    rebar3_path_deps
]}.

{deps, [
	{cowmachine, {path,"../.."}}
]}.

{escript_incl_apps,
 [launcher,hello_world]}.
 
{escript_main_app, launcher}.
{escript_name, launcher}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

